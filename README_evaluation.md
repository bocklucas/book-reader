# Evaluation Script
Run the script using your poetry or venv environment:
```bash
.venv/bin/python -m src.script_evaluate --dir local/output/mock_book --chapter 5
```
It implements verbatim checking and chunked sliding-window dialogue speaker evaluation.
